# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# PHP backend config (contains DB credentials, never commit)
api/config.php

# Dependencias de Composer — se resuelven con `composer install` a partir de
# composer.json/composer.lock (que sí se commitean); se incluyen ya resueltas
# en el zip de deploy, pero no en el repo.
api/vendor/
